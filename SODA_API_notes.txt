link for the Inpatient Psychiatric Facility Quality Measure Data – by Facility dataset API:
https://dev.socrata.com/foundry/data.medicare.gov/v7vx-dht4

Code to import it using sodapy:
client = Socrata("data.medicare.gov", None)
results = client.get("v7vx-dht4", limit=2000)
results_df=pd.DataFrame.from_records(results)


sodapy:
from sodapy import Socrata
client = Socrata("data.medicare.gov", None)
results = client.get("qk5r-kh7u", limit=5000)
results_df=pd.DataFrame.from_records(results)


metadat = client.get_metadata("r32h-32z5")
[x['name'] for x in metadat['columns']]
[x for x in metadat['columns'] if x['name']=='Measure Name'][0]
[x for x in metadat['columns'] if x['name']=='Score'][0]
results3 = client.get("r32h-32z5", where = "measure_name = 'Rate of readmission after discharge from hospital (hospital-wide)' AND score !='Not Available'", limit = 3000)
results3_df = pd.DataFrame.from_records(results3)


Areas with an IMU score of 62 or less are designated as MUAs. Some areas with higher scores, however, are still underserving certain specific populations (such as those with low-income, or non-English speakers). T



Sometimes, these outcome measures can come into conflict with each other. For instance, swift handoff can make patients feel like they aren't being given attention. So, patient satisfaction can be used as a balance measure for interventions aiming to affect another outcome measure. 

Certain interventions can reduce unplanned readmissions, such as:
 - medication reconciliation
 - follow-up phone calls
 - handoff to PCPs promptly after surgery


